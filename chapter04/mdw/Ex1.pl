#!/usr/bin/perl

use strict;
use warnings;

print "

    1,3,4)
    \$ginger is a ref to an array that has a ref as its third value which is to a an array with at least two values the second of which will be returned.
 

    \$ginger =  .-------(    )
                       (    )
                       ( . -)-----(                )
                                  ( returned value )

    2)

    \$ginger is an array that has a ref as its third value which is to a an array with at least two values the second of which will be returned.

    \$ginger =  (    )
               (    )
               ( . -)-----(                )
                          ( returned value )


</awesomeart>
";
