#!/usr/bin/perl

use strict;
use warnings;

print "Whoever wrote this mixed up hashes and hash references as they declare the passengers are hashes but by using curly brakets not parentisies(sp?) they assign the values as hash references.\n";
